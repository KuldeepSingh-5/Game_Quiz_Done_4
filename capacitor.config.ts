import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.DailyChallengeQuiz.app',
  appName: 'DailyChallengeQuiz',
  webDir: 'dist'
};

export default config;
